using DocumentFormat.OpenXml.Drawing;
using MiNET.Entities.Projectiles;

namespace Project2.src.Engine.Simulation.World
{
//    public struct Update
//    { 
//        float Drag = (int)-.5;
//        // Apply drag to velocity

//        Velocity -= readonly Velocity* Drag * deltaTime;
//        Position += readonly Velocity * deltaTime;

//  
//        Projectile projectile = new Projectile();

//        // Set the initial position and velocity of the projectile
//        projectile.Position = new Vector2(100, 100);
//        projectile.Velocity = new Vector2(10, 0);

//        // Update the projectile position over time
//        readonly float deltaTime = 0.1f; // Replace with your own delta time value
//        Projectile.Update(deltaTime);
//    }

}